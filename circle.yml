test:
  override:
    - echo 'all good'
  post:
    - git config --global user.email "heph@morgante.net"
    - git config --global user.name "heph"

deployment:
  production:
    branch: master
    commands:
      - npm run deploy
